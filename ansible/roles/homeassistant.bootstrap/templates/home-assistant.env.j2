# Home Assistant container image reference
HA_IMAGE={{ home_assistant_image | default('ghcr.io/home-assistant/home-assistant:stable') }}

# Optional bind mount for custom components (comment out if unused)
# CUSTOM_COMPONENTS_DIR={{ home_assistant_custom_dir | default('/var/lib/home-assistant/custom_components') }}
